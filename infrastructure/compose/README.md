# infrastructure/compose

Reserved for environment-specific Compose overlays (for example
`docker-compose.prod.yml` or `docker-compose.test.yml`) as the project grows
beyond the single root `docker-compose.yml` used for local development.
