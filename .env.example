# Used by root docker-compose.yml for variable substitution.
# Copy to `.env` before running `docker compose up`.

POSTGRES_USER=briefpilot
POSTGRES_PASSWORD=briefpilot
POSTGRES_DB=briefpilot
POSTGRES_PORT=5432

BACKEND_PORT=8000
FRONTEND_PORT=3000
