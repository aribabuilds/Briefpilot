# PROGRESS — BriefPilot M1–M30

**Milestones, not dates.** "Day N" in the execution plan = **Milestone N (MN)**. There is no
schedule pressure and no assumption about elapsed time. Source of truth for scope:
`briefpilot-30day-execution-plan.md.pdf` (in `../Briefpilot-decisions/`), governed by `CLAUDE.md`.

**Status legend:** `todo` · `in progress` · `done` · `blocked`

**Rule:** never start a milestone whose dependency isn't done. Critical path is
foundation → upload → OCR with bboxes → extraction → validators → highlight overlay → docs/demo.

---

## Sprint 1 (M1–M7) — "A letter goes in, real text comes out"

| M | Problem / user story | Status | Summary |
|---|----------------------|--------|---------|
| M1 | *As the maintainer,* I can run and verify the whole stack reproducibly, so every later feature stands on a foundation I can trust (and a recruiter can clone). | **done** | Monorepo scaffold, lint/format/type/test toolchain, **CI verified green**, pre-commit hooks, `Makefile` (`make dev`), `docs/adr/` + ADR-0001, backend boots and serves |
| M2 | *As a user,* I can upload a file and watch it process through to a result — so the whole path exists before any real analysis does. | **done** | `POST /api/v1/jobs` (upload) + `GET /api/v1/jobs/{id}` (poll) behind repo/service seam; landing + upload form + result page with live polling and honest states. Async job+poll wired now so the multi-second pipeline slots in without a rewrite. In-memory store (Postgres deferred). Verified end-to-end in-browser; 17 backend tests |
| M3 | *As a user,* I can upload a real photo or PDF of my letter and have its words read with their positions — so later steps get accurate, locatable text. | **done** | Ingestion (pypdfium2 rasterize, page-limit/corrupt guards) + `OcrService` ABC + `TesseractOcrService` producing the **frozen** normalized schema (`schemas/ocr.py`); ADR-0002. Bake-off resolved to Tesseract by §3 (see below). Normalization unit-tested locally; real Tesseract test runs in CI. Pipeline wiring deferred to M5 |
| M4 | *As a user,* I can photograph a slightly skewed or dimly lit letter and still get accurate text — I don't need a perfect flatbed scan. | **done** | `services/preprocess.py`: grayscale → conservative projection-profile deskew (skips trivial/saturated angles) → CLAHE contrast → bounded downscale, toggleable. Transforms unit-tested locally; measured OCR-confidence lift on a 12° skew runs in CI (fail-loud). OpenCV-headless (no system libs). Coords safe — preprocessing precedes OCR; ADR-0002 fractions survive downscale. Pipeline wiring still M5 |
| M5 | *As a user,* when I upload a letter I get back the actual extracted text of *my* document, not a placeholder. | **done** | `document_pipeline.build_document` (rasterize → preprocess → OCR per page) runs in a background thread pool off the request path; job flips to DONE with real text/summary or FAILED with an error. Per-page retry isolates one bad page, but **total failure surfaces** (all pages fail → `DocumentOcrError` → job failed, not a silent empty "done" — caught by running it locally). Thread-safe in-memory repo; OCR timeout. Verified in-browser (failed path renders cleanly); 45 tests, real OCR e2e in CI |
| M6 | *As a user,* if my photo is too poor to read, I'm told to retake it (with tips) instead of being handed silent garbage. | **done** | `services/quality.py` (pure `assess_quality`: word-count + mean-confidence thresholds) wired into `JobService` as a new terminal status `low_quality` — distinct from `failed` (OCR ran, output just isn't trustworthy). `RetakePrompt` shows confidence + photo tips and **deliberately withholds the OCR text**. Verified live (backend confirms status; browser renders the prompt with no console errors and no leaked text) via a temporary dependency-override server, since real low-confidence output needs a real bad photo. `eval/golden/` scaffolded (manifest + label format + README); **0 real letters collected yet** — that's the owner's parallel task, not fabricated. 60 tests pass locally |
| M7 | *As a user,* the upload→text journey works reliably on my actual phone. | **done** (Claude's half) | Bug sweep (`.gitattributes`, stale README intro, stale Known Deviations rows all fixed); bad-photo quality-gate test proven against real Tesseract in CI; LAN phone-test instructions in README; demo shot list + LinkedIn draft written. **Owner's half still open:** the phone test itself, the screen recording, publishing, and golden-letter collection — see Sprint-1 review below |

### Sprint-1 review

Scored against the plan's actual Sprint-1 goal: *"de-risk the single scariest dependency — OCR
quality on real-world photos — in week one"* via *"live walking skeleton + upload + preprocessing
+ OCR with bounding boxes + quality gate."*

| DoD item | Status | Note |
|---|---|---|
| Engineering objective met (skeleton + upload + preprocessing + OCR w/ bboxes + quality gate) | ✅ | M1–M6, all CI-verified |
| CI green | ✅ | Verified repeatedly; M6 even caught and fixed a real cross-milestone regression (see LEARNING.md) |
| Skeleton deployed | ❌ **by design** | ADR-0001 — zero-cost mandate; local `make dev` is the deploy target, not a live URL |
| 10 golden letters collected | ❌ **not started** | `eval/golden/` scaffolded (M6); 0 real letters — deliberately never Claude's task |
| Upload works on a real phone | ⏳ **untested** | README now documents the LAN-IP config needed (M7); the test itself needs the owner's phone |
| OCR JSON includes bboxes | ✅ | Frozen since M3 (ADR-0002): every `OcrWord` carries a fractional `BBox` |
| Quality gate triggers on a deliberately bad photo | ✅ | Proven against **real Tesseract** in CI as of M7 (previously only proven against a synthetic confidence dict) |

**Bottom line:** everything within Claude's control is done and verified. What's open —
golden letters, the phone test, the demo recording, the LinkedIn post — was always the owner's
half of Sprint 1, not slippage.

**Retro questions** (from the execution plan, for the owner — not graded, just asked):
1. Was the OCR bake-off decisive? (Note: it was resolved by the zero-cost mandate rather than a
   head-to-head benchmark — see ADR-0002. Does that feel like a real answer or a dodge?)
2. Is the 6–8h/day pace real, now that milestones are decoupled from calendar days?
3. What surprised you about real letters, once golden-set collection starts?

## Sprint 2 (M8–M14) — "The pipeline understands the letter"

| M | Problem / user story | Status | Summary |
|---|----------------------|--------|---------|
| M8 | *As a user,* the app recognizes what kind of letter I uploaded (Finanzamt, Krankenkasse, Bußgeld…), so it applies the right handling. | **done** | `classify_document` added to the `AIService` interface (all 3 adapters); new `GeminiService` (free tier) is now the **default** provider — resolves the paid-default deviation from M1. Response parsing is a pure, provider-shared function with an `other` fallback on any malformed output (never a guessed type). Wired into `JobService`: runs only after the quality gate passes, degrades to `doc_type: null` on any failure (no key, network, provider outage) without failing the job. ADR-0003. "Eval vs labeled set" deferred — still 0 golden letters, not fabricated. 77 tests pass locally |
| M9 | *As a user,* the important parts of my letter are captured in a structured form — each field carrying its confidence and where it came from — so results are trustworthy and traceable. | **done** | `LetterExtraction` — **one common schema for all 8 types**, not per-type (ADR-0004; deviates deliberately from the plan's literal "top-4 + generic" — no real letters yet to justify guessing per-type differences). Generic `ExtractedField[T]` wrapper (PEP 695 syntax; pydantic floor raised to 2.11) carries `{value, confidence, source_span}`; `source_span` reuses the frozen M3 `BBox`. Replaced the stale M1 `DocumentExtractionResult` placeholder (zero real callers) with this real contract. Shared, provider-agnostic parser never raises — malformed/missing fields degrade to null per-field. Prompt scaffolding only; wiring into `JobService` + real source-span linking is M10. 88 tests pass locally |
| M10 | *As a user,* I see the sender, dates, deadlines, amounts and required actions pulled out of my letter, each linked back to the words it came from. | **done** | `LetterExtraction` goes live: `JobService` runs extraction as a second best-effort step (independent of classification), then `link_source_spans` matches each value back to real OCR words. Matching splits into candidate generators (German date formats, comma/period amounts) + a pure word-window matcher — unlinkable values get their confidence **capped, not zeroed** (an ungrounded value must not look as trustworthy as a verified one). Verified live (both verified ✓ and unverified fields render correctly); CI-gated test proves linking finds real bboxes against real Tesseract output. 118 tests pass locally (6 skip without Tesseract) |
| M11 | *As a user,* I can trust the extracted dates/amounts/§-references because impossible values are caught and flagged, never silently shown as fact. | **done** | `services/validators.py`: pure, unit-tested checks — `deadline >= letter_date`, `amount >= 0`, `legal_references` against a curated § whitelist (`_KNOWN_LAW_CODES`). Failures never rewrite the value; they append a machine-readable code to a new additive `ExtractedField.validation_issues: list[str]` field (ADR-0005) and cap confidence at `0.2` — lower than M10's unverified-linking cap (`0.4`), since a self-contradictory value is a stronger signal than a merely-unlinked one. Wired into `JobService` right after `link_source_spans`; the two caps compose via `min()`, neither ever raises what the other lowered. Frontend `ExtractionSummary` renders a `⚠ flagged` badge with a tooltip, independent of the existing verified/unverified badge. Verified live via a temporary dependency-override server (deadline-before-letter-date, negative amount, and an unrecognized § reference all rendered correctly, values intact). 26 new validator tests + 2 new JobService integration tests; 164 tests pass locally (6 skip without Tesseract) |
| M12 | *As a hiring manager,* I can see published per-field accuracy on real letters — the quality is measured, not merely claimed. | **partial** | `eval/scoring.py`: a 5-outcome taxonomy per field — `correct` / `correct_null` / `missed` / `wrong` / `hallucinated`, not a pass/fail boolean — so the scorecard can tell "honestly said null" apart from "invented a value" (ADR-0006). `eval/run_eval.py` runs the real pipeline (OCR → classification → extraction → linking → validation) against every `eval/golden/manifest.json` entry and writes `eval/scorecard.md`; dependency-injected classifier/extractor (same seam as `JobService`) let a CI-gated e2e test prove it against a real rendered document without a live LLM call. Wired into CI as extra steps in the existing backend job (reuses its Tesseract + dev tools). 23 new eval tests pass locally, all real (2 require Tesseract, present on this machine as of M12 — see note below). **Blocked:** the actual baseline run and published accuracy numbers need real golden letters — still 0 in `manifest.json` (owner's task, cannot be fabricated per D15). Today's `scorecard.md` honestly says so instead of showing fake numbers. |
| M13 | *As a user,* extraction is accurate enough on the common letter types that I can rely on the deadlines it finds. | **blocked** | Entirely a measure-then-tune milestone (run `eval/run_eval.py`, read the scorecard, adjust the prompt, re-run) — there is nothing to build without real accuracy numbers to react to, and 0 golden letters exist. Not skipped: revisit the moment `eval/golden/manifest.json` has real entries |
| M14 | *As a user,* each field shows an honest confidence signal, so I know what to double-check. | **partial** | `frontend/src/lib/confidence.ts`: pure `confidenceTier()` bucketing raw `[0,1]` confidence into High/Medium/Low, thresholds (0.8/0.4) deliberately aligned with M10/M11's existing confidence caps rather than independently guessed. Wired into `ExtractionSummary` as color-coded confidence text (emerald/amber/orange), independent of and alongside the existing verified/unverified and flagged badges. Verified live twice: first against a synthetic fixture (all three tiers), then against the first fully real end-to-end run of the whole pipeline (real OCR + real Gemini classify/extract) once the 4 integration bugs above were fixed — HIGH-tier fields rendered emerald, a validator-flagged field rendered LOW/orange despite being source-verified, confirming the two signals compose correctly on genuine, non-synthetic data. **Blocked:** "golden set → 20" (still 0 letters) and the sprint retro (needs a completed sprint's worth of real usage to reflect on) are the owner's task |

## Sprint 3 (M15–M21) — "A human can use and verify it"

| M | Problem / user story | Status | Summary |
|---|----------------------|--------|---------|
| M15 | *As a user,* I get a plain-English explanation of my letter, grounded only in its own content — so I understand it without jargon and without invented legal advice. | **done** | Replaced the unused M1 `summarize` placeholder with a real `explain_document` operation across all 3 providers (`DocumentExplanationRequest{content, extraction}`). `prompts/explain.py` grounds strictly in letter text + extracted fields, forbids advice language. Two independent safeguards (ADR-0007): the prompt itself, plus a deterministic `services/advice_linter.py` that checks the model's *actual output* — never trusting the prompt alone. `services/readability.py` computes word count + Flesch Reading Ease against the ≤200-word/B1 target; violations are flagged, never silently truncated or rewritten. Wired into `JobService` as a third independent best-effort step, grounded on whatever extraction produced (even all-null). Frontend: persistent `Disclaimer` component + `ExplanationCard` with a visible ⚠ warning when advice phrases are found. Verified live: real pipeline confirmed OCR/classification/extraction working (until the free-tier 20-req/day quota was hit mid-session — expected, not a bug); frontend rendering verified via a synthetic fixture that deliberately included advice language, confirming the linter correctly flags it while preserving the original text. 34 new backend tests (readability, advice linter, explanation parsing, JobService wiring); 199 backend tests pass locally, 0 skipped |
| M16 | *As a user,* I get a deadline-sorted action checklist, and I can tap any unfamiliar German term for a plain definition. | **done** | `frontend/src/lib/checklist.ts`: pure `buildChecklist()` derives an urgency-flagged checklist from `required_actions` + the letter's single `deadline` — no new AI call, no schema change (the extraction schema has one deadline per letter, not one per action; sorting is a documented no-op today, tied to that shape, not an oversight). Urgent = within 14 days of *now*, computed at render time so it never goes stale on reload. `glossary.ts`: 58 curated, real Amtsdeutsch terms across all 8 letter types with plain-English definitions. `GlossaryText` finds and wraps every occurrence in rendered text (explanation + checklist items) as a tappable button showing an inline definition — no new backend involvement, purely a presentation feature (no ADR; same bar M14's confidence tiers didn't clear). Verified live: urgency flag fires correctly on a 5-day-out deadline; tapped "Steuerbescheid" and confirmed its definition renders inline |
| M17 | *As a user,* I see one clear results page — summary, explanation, checklist — that works on my phone, with honest loading and error states. | **done** | Largely a consolidation milestone: explanation (M15) and checklist (M16) already existed, so the new work was a `SummaryCard` (sender/deadline/amount at a glance, reusing M14's confidence-tier colors) placed at the top of the results view, plus a stale header rename ("Extracted text" → "Your letter", since the page now shows far more than raw text). Processing/done/low_quality/failed/error states were reviewed against this milestone's "honest" bar and found already correct from M2/M5/M6 — no changes needed. Verified live: `SummaryCard` renders correctly; confirmed **zero horizontal overflow at a real 375px mobile viewport** (`document.documentElement.scrollWidth` measured equal to `clientWidth`), full content parity between desktop and mobile renders |
| M18 | *As a user,* I can see the original scan of my letter rendered in the app. | **done** | New `DocumentStore` (mirrors `JobRepository`'s shape) persists the raw upload bytes — nothing survived past OCR before this milestone. New `GET /jobs/{id}/pages/{n}` reuses `ingestion.rasterize()` (the same function OCR's own first step uses) to serve the RAW, un-preprocessed page as PNG — not the grayscale/deskewed OCR-preprocessing image, a deliberate choice (ADR-0008) so what's shown is recognizably the user's actual photo. Frontend `DocumentViewer` renders one `<img>` per page and, using a new `bbox.ts` (fractional `BBox` → CSS `%`, correct at any render size with zero pixel math), draws a faint permanent overlay for every field with a resolved `source_span` — real extraction data, proving the M18 coordinate-math deliverable, though the actual tap-to-highlight *interaction* is explicitly M19's job. Verified live end-to-end: uploaded a real letter, confirmed the served PNG (3000×1500) matched what the browser actually rendered (`img.naturalWidth/Height`), and confirmed a synthetic field's bbox rendered at the exact CSS position its fraction predicts (`x=0.35 → left: 35%`). 209 backend tests pass, 10 new (`DocumentStore` + endpoint) |
| M19 | *As a user,* I can tap an extracted field and see exactly where it appears, highlighted in my original letter — proof the AI didn't invent it. | **done** | `ExtractionSummary` rows became real, clickable buttons (`selectedKey`/`onSelect` props). Selection state lifted into a new `DoneView` component (split out of `renderState`, a plain function that can't hold hooks) since `ExtractionSummary` and `DocumentViewer` are siblings needing the same state. `DocumentViewer` now accepts `selectedField`: a verified field gets a distinct pulsing highlight (on top of M18's faint permanent boxes) and the viewer auto-scrolls its page into view via a per-page ref map; an unverified field (`source_span: null`) shows a "could not be matched — verify manually" prompt instead of pretending to point somewhere. Multi-page: scroll targets the specific page a field's span is on, not just page 0. Verified live: clicking a verified field produced the exact predicted highlight position; clicking an unverified field produced no highlight and the correct prompt text, with `aria-pressed` correctly toggling in both cases. ADR-0008's deskew-alignment question remains explicitly open — still no real letters to supply the evidence it calls for |
| M20 | *As a user,* the full journey keeps working from one release to the next. | **done** | New `test_full_pipeline_e2e.py`: real Tesseract OCR feeding classification, extraction (+ linking + validation), and explanation (+ readability + advice-linter) all in one job — every earlier e2e test covered a slice (OCR alone, or OCR+extraction); none proved all four stay correctly wired together. First Playwright test in the project (`@playwright/test`, Chromium): `e2e/happy-path.spec.ts` mocks every backend call via route interception (health, upload, poll, page image) — no live backend, Tesseract, or Gemini quota needed, a fast fully-deterministic check of the actual rendered app across the real upload → poll → results → tap-to-highlight → navigate-back journey. 3 specs, all passing locally. Wired into CI as new steps in the existing frontend job. Also: found and fixed 4 non-breaking `npm audit` vulnerabilities surfaced while adding the dependency; flagged the 3 remaining (needing a Next.js major-version bump) as a separate task rather than doing a breaking upgrade silently mid-milestone. 210 backend tests pass |
| M21 | *As a real non-native user,* I can complete the whole journey on my phone without getting confused. | **blocked** | Needs 2 real non-native testers on real phones — cannot be simulated or fabricated (same principle as D15's golden-letter rule; a guessed friction point would be worse than none). `docs/m21-phone-test-script.md` prepared: recruiting guidance, a hands-off task list, a friction-log template, and next steps for turning results into a top-3 fix list. LAN phone-testing setup (M7) already in place. Owner's task to run the actual sessions; Claude picks up the top-3 fixes once she has them |

## Sprint 4 (M22–M30) — "Ship, harden, and tell the story"

| M | Problem / user story | Status | Summary |
|---|----------------------|--------|---------|
| M22 | *As a user,* I can delete my document in one click and know it's really gone (and auto-purged within 24h) — no account required. | **done** | `JobRepository`/`DocumentStore` gained `delete()` (+ `list_all()` for the sweep); new pure `services/retention.py::purge_expired()` computes expired job ids from an injected `now`/`max_age`, fully unit-tested without a real clock. `DELETE /jobs/{id}` (204/404) removes both the job record and the raw document bytes. An in-process `asyncio` background task in `main.py`'s FastAPI `lifespan` sweeps every `retention_sweep_interval_seconds` (default hourly) purging anything past `retention_max_age_hours` (default 24) — no cron, no external scheduler, per ADR-0009. Frontend `DeleteButton`: two-stage confirm (not a full modal, not a bare unconfirmed click), and after DELETE succeeds it re-fetches the job and only claims "deleted" once that returns 404 — verifying the privacy claim at the client too, not just trusting the DELETE response. Verified live against a real running backend (curl): page served 200 → delete 204 → job 404 → page 404 → second delete 404; also caught a real race for free — the background OCR pipeline finished *after* delete and tried to write `low_quality` back, and the pre-existing `if job is None: return` guard in `JobService._update` correctly discarded it. 18 new backend tests (228 total), 2 new Playwright specs (5 total) |
| M23 | *As a user,* I can read, in plain language, exactly what happens to my data — and it matches what the code actually does. | **done** | New static `/privacy` page, written to describe M22's actual behavior line by line: in-memory-only storage (gone immediately on a restart, not just after 24h), the honest hourly-sweep cadence ("usually a little before 24h, never meaningfully after"), one-click delete, and — the one disclosure that would've been dishonest to omit — that the letter's OCR'd **text** (never the original image) is sent to Google's Gemini API for classification/extraction/explanation, a real third party. Also states plainly that this is a portfolio project, not an operated commercial service. Linked from the landing page footer and every result page. No ADR (presentation/copy, same bar M16/M17 didn't clear). Landing copy pass: tagline and upload-flow blurb reworded, footer link states the two claims (no account, 24h auto-delete) up front rather than making a user click through to learn them. Verified live at 375px mobile (zero horizontal overflow) and via a new Playwright spec (`e2e/privacy.spec.ts`) proving both real navigation paths and that the Gemini disclosure specifically renders. 6 e2e specs total |
| M24 | *As a user,* the service stays responsive and safe even under load or abusive input. | **done** | **Rate limiting:** new in-memory per-IP sliding-window `RateLimiter` (`services/rate_limiter.py`), wired onto `POST /jobs` and `DELETE /jobs/{id}` (20/min default); live-verified against a real running server — 20 requests handled normally, the 21st/22nd got `429`. **Size guards:** upload reads now happen in bounded 1 MiB chunks (`_read_bounded`) that reject the moment the running total exceeds `max_upload_bytes`, instead of buffering an arbitrarily large body before checking. **Structured logging:** a new `log_requests` middleware emits one `http_request` event per request (method/path/status/duration/client IP) through the existing M1 structlog pipeline; live-confirmed in the real server's log output. **Guardrails:** all three LLM prompts (classify/extract/explain) now delimit the letter's OCR'd text and explicitly instruct the model never to treat it as instructions (`services/ai/prompts/__init__.py`) — defense-in-depth alongside, not instead of, M11/M15's existing output-side validators. **Uptime monitor: deliberately not built** — per ADR-0010, there's no hosted deployment to watch (ADR-0001), so standing one up against `localhost` would be infrastructure theater; revisit at M29 if a free hosting tier is ever proposed and approved. ADR-0010. 17 new backend tests (245 total) |
| M25 | *As a hiring manager,* I can read an honest account of what the system gets wrong and why. | todo | Full eval on 30 golden letters; honest failure analysis; freeze scorecard |
| M26 | *As a hiring manager,* I can understand the architecture and reproduce the project from the README in minutes. | todo | Architecture diagram, portfolio README rewrite, ADR index |
| M27 | *As a hiring manager,* I can watch a 3-minute demo of the full journey without running anything myself. | todo | 3-minute demo video, screenshot set |
| M28 | *As a new user or developer,* I can follow the README on a clean machine and it just works. | todo | Follow README on fresh environment, fix gaps, triage bugs |
| M29 | *As the maintainer,* I can confidently declare the MVP live and monitored. | todo | Full launch checklist, monitor verification |
| M30 | *As the owner,* I can share the finished project and its lessons publicly. | todo | Launch post, `docs/retro.md`, post-MVP Sprint-5 candidates |

---

## Zero-cost adaptations (CLAUDE.md §3 overrides the execution plan)

The execution plan assumes a funded project. `CLAUDE.md` §3 is a **hard rule** and wins where they conflict:

- **M1** — plan says "EU-region deploy target (Hetzner/Fly EU or Vercel fra1) + Sentry EU."
  Adapted: **no hosting is provisioned.** Demo strategy is local (`make dev` / Docker Compose)
  + recorded video + a README a recruiter can follow on a clean machine. A free tier may be
  *proposed* later, never silently provisioned.
- **M3** — plan says bake off Azure Document Intelligence vs Google Vision vs Tesseract.
  Adapted: **Tesseract is the default** for dev, CI, and demo; the paid providers were not
  provisioned or benchmarked (doing so would itself violate §3). The `OcrService` adapter is
  designed so a paid provider is a config/adapter swap later. **ADR-0002** records the decision
  and freezes the coordinate schema.
- **M8+** — LLM must run on a **free tier**. **Resolved at M8**: `GeminiService` (Gemini Flash
  free tier) is now the default `AI_PROVIDER`; OpenAI/Azure remain opt-in only. ADR-0003.
- **M29** — no paid domain/DNS/VPS. Launch = public repo + video + reproducible local run.

## Known deviations & carried debt

| Item | Impact | Where it gets resolved |
|------|--------|------------------------|
| ~~CI workflow triggers on `main`; repo branch is `master` — CI has never run~~ | **RESOLVED.** Branch renamed to `main` (D1); `ci.yml` needed no edit. First run failed (black + mypy in `core/logging.py`); fixed in `45a8b59`. **Run #3 green: Frontend 47s, Backend 31s** | M1 ✅ |
| ~~`AIService` ships only **OpenAI + Azure OpenAI** adapters — both paid~~ | **RESOLVED.** `GeminiService` added; `AI_PROVIDER` now defaults to `gemini` (free tier). Paid providers are opt-in only | M8 ✅ (ADR-0003) |
| ~~Wrapper is named `AIService`; CLAUDE.md §4 calls it `llm_client`~~ | **RESOLVED.** Kept `AIService`: by M8 it's a 3-operation, 3-provider capability interface, not a single-call client wrapper. ADR-0003 documents the reasoning | M8 ✅ (ADR-0003) |
| ~~`GEMINI_API_KEY` not yet obtained~~ | **RESOLVED**, and surfaced 4 real bugs the first real key exposed (all fixed same session, see LEARNING.md's M14 post-merge fix): a shared async client reused across event loops, a retired model name (twice), `response_mime_type="application/json"` not reliably honored, and a latent test-isolation gap in `test_pipeline_e2e.py`. First real end-to-end run (OCR → classify → extract → link → validate) verified live | Owner ✅ + Claude ✅ |
| ~~Tesseract not installed on the owner's native (non-Docker) machine~~ | **RESOLVED.** Installed (2026-08-13, 5.5.3) — all 6 previously CI-only, Tesseract-gated tests now also pass locally; **165/165 backend tests, 23/23 eval tests, 0 skipped** | Owner ✅ |
| § whitelist doesn't recognize spelled-out "Paragraph" (only the `§` symbol) | Real letter text ("Paragraph 152 AO") correctly flagged `unrecognized_legal_reference` — the flagging logic worked, the whitelist regex is genuinely incomplete | M13 (real-data tuning), once real letters exist |
~~Uploaded document bytes now persist in-memory for the process's lifetime, with no expiry (M18's `DocumentStore`)~~ | **RESOLVED.** `delete()` on both `JobRepository`/`DocumentStore` + an in-process `asyncio` sweep purge anything past `retention_max_age_hours` (default 24h, hourly cadence) — ADR-0009. Still in-memory-only (`InMemoryJobRepository`/`InMemoryDocumentStore` remain the only implementations, per ADR-0001), so a process restart still clears everything immediately — a stronger, not weaker, privacy property than the 24h ceiling promises | M22 ✅ (ADR-0009) |
| ~~No `pre-commit` hooks~~ | **RESOLVED.** `.pre-commit-config.yaml`, all hooks verified passing on the full tree | M1 ✅ |
| ~~No `docs/adr/` directory~~ | **RESOLVED.** `docs/adr/` + index + ADR-0001 (local-first zero-cost strategy) | M1 ✅ |
| ~~No `LEARNING.md`~~ | **RESOLVED.** M1 review with 4 decisions, 3 review questions, teach-back | M1 ✅ |
| ~~Frontend source never prettier-formatted~~ | **RESOLVED.** CI ran `lint` + `build`, which both pass on unformatted code; `format:check` added to CI and 3 files reformatted | M1 ✅ |
| `make` not installed on the owner's Windows machine | `make dev` won't run locally until `choco install make`; Makefile targets are thin wrappers, documented in README | Owner's call |
| ~~Backend never executed locally~~ | **RESOLVED.** Full backend job now runs locally (Python 3.14 venv) and in CI: ruff, black, isort, mypy strict all clean; **9 tests pass**, incl. the `AIService` factory tests. Note local 3.14 vs project target 3.13 — Docker/CI remain the source of truth | M1 ✅ |
| ~~Existing commits don't follow conventional-commit format~~ | **HOLDING.** Verified: every commit from M2 onward (`feat:`/`fix:`/`test:`/`docs:`/`chore:`) has followed the convention; history before that isn't rewritten | Ongoing, self-verified at M7 |
| ~~Every commit warns "LF will be replaced by CRLF"~~ | **RESOLVED.** Flagged after M2, never actually fixed until this bug sweep caught it. Added `.gitattributes` (`* text=auto eol=lf`); verified the warning no longer fires on `git add` | M7 ✅ |
| Sprint-1 DoD item "10 golden letters collected" — **0 collected** | `eval/golden/` scaffold exists (M6) but is empty; this was deliberately never Claude's task (fabricating letters would produce a dishonest scorecard) | Owner, ongoing — not blocking Sprint 2 |
| Sprint-1 DoD item "upload works on a real phone" — **never tested** | No phone + LAN test has been run; `NEXT_PUBLIC_API_URL` bakes `localhost` at build time, which breaks from an actual phone unless overridden — documented in README (M7) | Owner — see README "Testing on a real phone" |
| M1–M10 `LEARNING.md` review questions — **unanswered** | CLAUDE.md §6 says not to proceed to the next milestone until the owner engages with them; the owner explicitly chose "full speed, review later" for M11 onward (2026-08-13), so questions are accumulating across milestones by design, not oversight | Owner, in one batch, whenever she stops to review |
| M12/M13 (published accuracy, prompt iteration) need real golden letters | Still 0 collected — cannot be fabricated (D15, and CLAUDE.md's own eval-integrity principle); this blocks M12/M13's actual measurement work even in auto mode | Owner's parallel task — flagged explicitly when M12 is reached, not silently skipped |
